# JustInCase Backend

Minimal NestJS backend scaffold for the JustInCase project.

Setup

```bash
cd src/backend
yarn install
yarn dev
```

Endpoints

- `GET /` → basic welcome message
- `GET /health` → health check

Follow `AGENT.md` for repo conventions (TypeScript, linting, tests).
